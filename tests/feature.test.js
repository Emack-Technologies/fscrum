const {test} = QUnit;
QUnit.module("Feature");


test("throws an exception when the feature name is not precise", assert=>{

});

test("feature creation", assert=>{

});

test("visual representation of a feature", assert=>{});


/**
 * Tests related to setters
 */
test("set description", assert=>{});

test("set status", assert=>{});


/**
 * Tests related to getters
 */
test("get description", assert=>{});

test("get status", assert=>{});


/**
 * Tests related to adding a story to the feature
 */

test("includes(story) - throws an exception when the parameter is not a story object", assert=>{});

test("includes(story) - add a story to the feature", assert=>{});

/**
 * Tests related to removing a story from the feature
 */
test("removeStory(id) - remove a story from a feature", assert=>{});

